<header>
  <nav>
    <a href="/" class="glass-button">home</a>
    <a href="/about" class="glass-button">about</a>
    <a href="/blogs" class="glass-button">blog</a>
    <a href="https://notecard-public.vercel.app/" class="glass-button ai-pinwheel-button">notecards</a>
    <!--a href="/ai-message" class="glass-button ai-pinwheel-button">
      <span class="ai-text-full">message to ai</span>
      <span class="ai-text-short">ai</span>
    </a>-->

  </nav>
</header> 

<style>
  header {
    position: fixed;
    top: 0;
    right: 0;
    width: 100%;
    padding: 2rem;
    display: flex;
    justify-content: right;
    z-index: 10;
  }

  nav {
    display: flex;
    gap: 2rem;
  }

  .glass-button {
    padding: 1rem 1.5rem;
    color: black;
    text-decoration: none;
    font-size: 1rem;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgb(0, 0, 0);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
  }

  .glass-button:hover {
    background: rgba(83, 83, 83, 0.2);
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
  }

  .ai-pinwheel-button {
    position: relative;
    overflow: hidden;
  }

  .ai-text-short {
    display: none;
  }

  .ai-pinwheel-button::before {
    content: '';
    position: absolute;
    top: -150%;
    left: -150%;
    width: 400%;
    height: 400%;
    background: conic-gradient(
      from 0deg,
      #ff0000,
      #ff8000,
      #ffff00,
      #80ff00,
      #00ff00,
      #00ff80,
      #00ffff,
      #0080ff,
      #0000ff,
      #8000ff,
      #ff00ff,
      #ff0080,
      #ff0000
    );
    border-radius: 100%;
    animation: pinwheel-spin 2s linear infinite;
    filter: blur(.8px);
    z-index: -1;
    opacity: .5;
  }

  .ai-pinwheel-button::after {
    content: '';
    position: absolute;
    top: -150%;
    left: -150%;
    width: 400%;
    height: 400%;
    background: conic-gradient(
      from 45deg,
      #ff4444,
      #ff9944,
      #ffff44,
      #99ff44,
      #44ff44,
      #44ff99,
      #44ffff,
      #4499ff,
      #4444ff,
      #9944ff,
      #ff44ff,
      #ff4499,
      #ff4444
    );
    border-radius: 100%;
    animation: pinwheel-spin 2s linear infinite reverse;
    filter: blur(0.8px);
    z-index: -1;
    opacity: 0.5;
  }

  @keyframes pinwheel-spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @media (max-width: 768px) {
    header {
      padding: 0.5rem 0.75rem;
    }
    
    nav {
      gap: 0.5rem;
      flex-wrap: wrap;
      justify-content: flex-end;
    }
    
    .glass-button {
      padding: 0.4rem 0.6rem;
      font-size: 0.8rem;
    }

    .ai-pinwheel-button {
      position: relative;
    }

    .ai-text-full {
      display: none;
    }

    .ai-text-short {
      display: inline;
    }

    .ai-pinwheel-button::before {
      filter: blur(0.8px);
    }

    .ai-pinwheel-button::after {
      filter: blur(0.6px);
    }
  }

  @media (max-width: 480px) {
    nav {
      gap: 0.3rem;
    }
    
    .glass-button {
      padding: 0.3rem 0.5rem;
      font-size: 0.75rem;
    }
  }
</style> 